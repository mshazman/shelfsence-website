# shelfsence-website

Static ShelfSense marketing site (GitHub Pages).

## Live site

Enable **Settings → Pages → Deploy from branch → `main` / root**, or use GitHub Actions if configured.

## Local preview

```bash
python3 -m http.server 8080
```

Open http://localhost:8080/
